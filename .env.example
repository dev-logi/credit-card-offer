# Application Settings
APP_NAME=Credit Card Recommendation Service
APP_VERSION=1.0.0
DEBUG=True

# Database Configuration
# For local development (SQLite) - default, no changes needed:
DATABASE_URL=sqlite:///./credit_cards.db

# For production (PostgreSQL via Supabase):
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# CORS Settings
# For local development, "*" allows all origins
# For production, specify your frontend domain:
# CORS_ORIGINS=["https://your-frontend.com", "https://www.your-frontend.com"]
CORS_ORIGINS=["*"]

# API Configuration
API_PREFIX=
DEFAULT_TOP_N=3
DEFAULT_REFERENCE_AMOUNT=100.0
